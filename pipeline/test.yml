platform: linux

image_resource:
    type: docker-image
    source:
        repository: microsoft/dotnet
        tag: 1.1.0-sdk-msbuild-rc4

inputs:
    - name: code

run:
    dir: code/test
    path: sh
    args:
    - -exc
    - |
      dotnet restore
      dotnet test ./**/*.csproj