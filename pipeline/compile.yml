platform: linux

image_resource:
    type: docker-image
    source:
        repository: microsoft/dotnet

inputs:
    - name: code

outputs:
    - name: build

run:
    dir: code
    path: sh
    args:
    - -exc
    - |
      dotnet restore
      dotnet build